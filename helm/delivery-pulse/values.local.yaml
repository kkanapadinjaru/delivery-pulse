# values.local.yaml — Docker Desktop / local Kubernetes
# Used by: deploy.sh install local

config:
  keycloakAuthority: "http://acr-manager-fabric-keycloak-service.acr-manager.svc.cluster.local:8080/auth/realms/delivery-pulse"
  keycloakPublicUrl: "http://localhost:18080/auth"

ui:
  service:
    type: NodePort
    nodePort: 30080
